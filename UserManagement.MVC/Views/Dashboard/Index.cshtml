@{
    ViewData["Title"] = "Dashboard";
}

@{
    ViewData["Title"] = "Dashboard";
    var userName = ViewBag.UserName as string;
    var userRole = ViewBag.UserRole as string;
}

<div class="alert alert-info d-flex justify-content-between align-items-center">
    <div>
         Welcome, <strong>@Context.Session.GetString("UserName")</strong>!
    </div>
    <div>
        @{
            var role = Context.Session.GetString("UserRole");
        }

        @if (role == "Admin")
        {
            <a class="btn btn-primary btn-sm me-2" href="/User/All">Manage Users</a>
            <a class="btn btn-success btn-sm me-2" href="/User/Add">Add User</a>
        }

        <a class="btn btn-danger btn-sm" href="/Account/Logout">Logout</a>
    </div>
</div>

